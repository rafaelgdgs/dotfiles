Needed packages: fd, ripgrep, fish, starship, eza, fisher, tealdeer, xclip, net-tools, inetutils, nmap, btop, qbittorrent, kitty, wget, curl, vlc, unrar, unzip, openvpn, network-manager-applet, network-manager-openvpn, git, docker, docker-compose, xf86-video-dummy, earlyoom, fzf, bat, gnome-shell-extension-appindicator, vulkan-tools
graphics card: vulkan-radeon


-- The commitizen command line utility.

sudo npm install -g commitizen
npm install -g cz-conventional-changelog
echo '{ "path": "cz-conventional-changelog" }' > ~/.czrc

pacman -Qdtq | pacman -Rns -
pacman -D --asexplicit package
pacman -Qo package # Show package owner of a command
pacman -Ql package # List files installed with package

ctrl + alt + F # Search directory
ctrl + alt + L # Search Git Log
ctrl + alt + S # Search Git status
ctrl + R # Search history
ctrl + alt + P # Search Processes
ctrl + V # Search Variables


sudo compsize -x /

rustup default stable # Define the default toolchain
rustup toolchain install nightly # Install a toolchain
rustup target list # List available targets
rustup target add X # Add x target
cargo new hello_world
rustup component add rust-analyzer
rustup component add rust-src
rustup component add clippy
rustup component add rustfmt
~/.cargo/config.toml:
[target.$ARCH-pc-windows-gnu]
linker = "/usr/bin/$ARCH-w64-mingw32-gcc"
ar = "/usr/bin/$ARCH-w64-mingw32-ar"


SHOW `columns` FROM `your-table`;
CREATE USER 'username'@'host' IDENTIFIED WITH authentication_plugin BY 'password';
CREATE USER 'sammy'@'localhost' IDENTIFIED BY 'password';
RENAME USER 'sammy'@'localhost' TO 'sammy'@'remote_server_ip';
https://www.digitalocean.com/community/tutorials/how-to-create-a-new-user-and-grant-permissions-in-mysql
https://www.digitalocean.com/community/tutorials/how-to-allow-remote-access-to-mysql

git reset # unstage all 'git add' files
git reset HEAD~ # undo last commit
git checkout . # rever all local uncommited changes
git clean -fdx # remove all local untracked files
https://stackoverflow.com/questions/927358/how-do-i-undo-the-most-recent-local-commits-in-git
git branch --set-upstream-to=origin/main main

alias -s lsl "ls -l" # example

xdotool click --repeat [number of times to repeat] --delay [number of milliseconds between clicks] 1

lsattr # List file attributes

Connect to a local multiplexer server over unix domain sockets => https://wezfurlong.org/wezterm/features.html#available-features

git config --global init.defaultBranch main => change default git branch name

starship preset nerd-font-symbols -o ~/.config/starship.toml


git init --bare ~/.dotfiles
alias dotfiles='/usr/bin/git --git-dir="$HOME/.dotfiles/" --work-tree="$HOME"'
(IF ON FISH)-> fish: funcsave dotfiles
dotfiles config status.showUntrackedFiles no

git clone --bare <git-repo-url> $HOME/.dotfiles
alias dotfiles='/usr/bin/git --git-dir="$HOME/.dotfiles/" --work-tree="$HOME"'
(IF ON FISH) -> fish: funcsave dotfiles
dotfiles checkout
dotfiles config --local status.showUntrackedFiles no
